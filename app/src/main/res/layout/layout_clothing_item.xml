<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/standard_margin">
    <ImageView
        android:id="@+id/clothing_image"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:src="@drawable/pic1_cloth"
        android:adjustViewBounds="true"
        android:scaleType="centerCrop"/>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@id/clothing_image"
        android:padding="@dimen/standard_margin_x2">
        <TextView
            android:id="@+id/clothing_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="@dimen/standard_margin"
            tools:text="عنوان لباس"
            android:textColor="@color/white"
            android:textSize="@dimen/large_font_size"/>
        <TextView
            android:id="@+id/clothing_view_count_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/clothing_title"
            android:layout_alignParentRight="true"
            tools:text="700"
            android:textColor="@color/white"
            android:textSize="@dimen/large_font_size"/>
        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="@dimen/standard_margin"
            android:layout_alignTop="@id/clothing_view_count_text"
            android:layout_alignBottom="@id/clothing_view_count_text"
            android:layout_toLeftOf="@id/clothing_view_count_text"
            android:src="@drawable/ic_action_arrow_back" />
    </RelativeLayout>
</RelativeLayout>